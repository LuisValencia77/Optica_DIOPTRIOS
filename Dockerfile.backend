FROM node:22-alpine

WORKDIR /app

# Copiar dependencias (package.json está en la raíz)
COPY package*.json ./

# Instalar dependencias
RUN npm ci

# Copiar el código del servidor
COPY server/ ./server/

# Puerto del backend
EXPOSE 4000

# Iniciar servidor
CMD ["node", "server/index.js"]
